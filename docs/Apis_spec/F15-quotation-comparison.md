# F15 QuotationComparison (API v1)

Status: **pending**. Do not implement. Wait until this file is ready (backend ships this ID).

```json
{
  "QuotationComparison": {
    "status": "pending",
    "id": "F15",
    "apisback": "Pending.",
    "apisfront": {},
    "apisspec_plan": "Compare + select / clear selection. quotation-comparison.* permissions."
  }
}
```
